// print 'good', if string starts with letter 'a' and has length greater than 3, otherwise print 'no'.


let str = 'apple';

if((str[0] === 'a') && (str.length > 3))
    console.log('good');
else
    console.log('no');