let name = "Bharat";

// find length of the name
console.log(name.length);

// print first and last character of the name
console.log(name[0], name[name.length-1]);

// string concatenation
name = name + 123;
console.log(name);

let empty = '';
let space = " ";
console.log(empty, empty.length);
console.log(space, space.length);